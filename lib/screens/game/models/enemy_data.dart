class EnemyData{
 final double speed;
 final int id;
 final int level;
 final bool hMove;
 final int killPoint;

 const EnemyData( {required this.speed, required this.id, required this.level, required this.hMove, required this.killPoint});
}